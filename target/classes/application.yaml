server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/driques/config-data
#          search-paths: config-data
  application:
    name: config-service
